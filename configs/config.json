# Stores configuration for the model, data, and training.
{
    "batch_size": 64,
    "learning_rate": 0.001,
    "num_epochs": 10,
    "patch_size": 16,
    "num_heads": 8,
    "embed_dim": 512,
    "dropout_rate": 0.1,
    "dataset_path": "path/to/dataset",
    "validation_split": 0.1,
    "num_workers": 4
}
